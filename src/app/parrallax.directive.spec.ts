import { ParrallaxDirective } from './parrallax.directive';

describe('ParrallaxDirective', () => {
  it('should create an instance', () => {
    const directive = new ParrallaxDirective();
    expect(directive).toBeTruthy();
  });
});
